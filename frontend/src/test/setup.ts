import '@testing-library/jest-dom/vitest'

// jsdom doesn't implement matchMedia; sonner's Toaster uses it for system theme detection.
if (!window.matchMedia) {
  window.matchMedia = (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
  })
}
